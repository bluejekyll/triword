# Calculates grids of Words

## Install Rust

```console
$> curl https://sh.rustup.rs -sSf | sh
```

## Run the program

The `--release` optimizes the program. 

```console
$> cargo run --release
```
